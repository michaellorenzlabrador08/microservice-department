server:
  port: 8079
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_profile?serverTimezone=UTC
    username: root
    password: mikeemon123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true

